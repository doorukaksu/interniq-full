# Development and testing dependencies
# Install with: pip install -r requirements-dev.txt
-r requirements.txt

pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
cryptography>=42.0   # RSA key generation for clerk_auth tests
